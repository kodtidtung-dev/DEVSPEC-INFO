<svg width="800" height="600" xmlns="http://www.w3.org/2000/svg">
  <!-- Background gradient -->
  <defs>
    <linearGradient id="grad3" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:rgba(255,51,51,0.18);stop-opacity:1" />
      <stop offset="100%" style="stop-color:rgba(255,51,51,0.06);stop-opacity:1" />
    </linearGradient>
  </defs>

  <!-- Background -->
  <rect width="800" height="600" fill="url(#grad3)"/>

  <!-- Large letter -->
  <text x="400" y="350" font-family="Inter, sans-serif" font-size="200" font-weight="bold" fill="rgba(74,74,74,0.2)" text-anchor="middle">D</text>

  <!-- Project name -->
  <text x="400" y="450" font-family="Inter, sans-serif" font-size="24" fill="rgba(74,74,74,0.5)" text-anchor="middle">Dorm Management</text>
</svg>